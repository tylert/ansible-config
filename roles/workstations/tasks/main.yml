---
- include: gqrx.yml

- name: install useful clicky packages
  apt: pkg="{{ item }}" state=present
  with_items:
    - gitk
    - build-essential
    - keepassx
    - libreoffice
    - librecad
    - inkscape
#    - blender
#    - vlc
#    - sshfs
#    - virtualbox
