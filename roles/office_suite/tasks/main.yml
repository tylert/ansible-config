---

- name: Install libreoffice
  apt:
    pkg: '{{ item }}'
    state: present
  loop:
    - libreoffice
    - libreoffice-pdfimport
  when: graphical

- name: Remove abiword and gnumeric
  apt:
    pkg: '{{ item }}'
    state: absent
    purge: yes
  loop:
    - abiword
    - gnumeric
